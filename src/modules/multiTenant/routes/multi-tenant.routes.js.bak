const express = require('express');
const router = express.Router();
const controller = require('../controllers/multi-tenant.controller');

router.get('/status', controller.status);

module.exports = router;
