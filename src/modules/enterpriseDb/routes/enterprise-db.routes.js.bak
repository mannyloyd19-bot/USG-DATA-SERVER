const express = require('express');
const router = express.Router();
const controller = require('../controllers/enterprise-db.controller');

router.get('/summary', controller.summary);

module.exports = router;
