const express = require('express');
const router = express.Router();
const controller = require('../controllers/api-key-analytics.controller');

router.get('/', controller.summary);

module.exports = router;
